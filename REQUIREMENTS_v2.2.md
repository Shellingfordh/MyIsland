# REQUIREMENTS v2.2 (Flux + Tray)

## Goals
1. **Flux‑style controls** via Night Shift (toggle, temp, schedule, movie mode).
2. **Notch Productivity Tray** quick actions (clipboard, downloads, desktop, AirDrop).
3. **Settings entry**: a single right‑click menu to access all controls.

## Scope
- `plugins/island.sh`: add Flux controls + tray actions.
- `README` (EN/中文): update highlights and preview.

## Acceptance
- Flux controls reachable in right‑click menu.
- Tray actions execute and show a pill confirmation.
